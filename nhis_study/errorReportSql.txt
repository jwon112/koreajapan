DBMS:
sql server

Error:
com.microsoft.sqlserver.jdbc.SQLServerException: 개체 이름 'dbo.death' 및 인덱스 이름 'xpk_death'에 키가 중복되므로 CREATE UNIQUE INDEX 문이 종료되었습니다. 중복 키 값은 (10000655)입니다.

SQL:
IF NOT EXISTS (
    SELECT 1 FROM sys.key_constraints
    WHERE parent_object_id = OBJECT_ID('death')
      AND type = 'PK'
      AND name = 'xpk_death'
)
BEGIN
    ALTER TABLE death ADD CONSTRAINT xpk_death PRIMARY KEY NONCLUSTERED ( person_id ) ;
END;

R version:
R version 4.5.2 (2025-10-31 ucrt)

Platform:
x86_64-w64-mingw32

Attached base packages:
- stats
- graphics
- grDevices
- utils
- datasets
- methods
- base

Other attached packages:
- etlKoreanNSC (0.9.0)
- SqlRender (1.19.4)
- DatabaseConnector (7.0.0)